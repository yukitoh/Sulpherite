/// <reference types="bluebird" />
import Promise = require('bluebird');
declare const _default: ((url: string, port?: number | undefined) => Promise<number>) & {
    default: (url: string, port?: number | undefined) => Promise<number>;
};
export = _default;
